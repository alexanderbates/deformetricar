% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deformetrica4.R
\name{deformetrica_register_multi}
\alias{deformetrica_register_multi}
\title{Fit ONE diffeomorphism to many matched objects (multi-object registration)}
\usage{
deformetrica_register_multi(
  sources,
  targets,
  kernel_width,
  object_kernel_width = kernel_width,
  landmarks = NULL,
  data_sigma = 0.5,
  landmark_sigma = mean(data_sigma),
  timepoints = 10L,
  max_iterations = 150L,
  device = c("auto", "cpu", "cuda"),
  deformetrica = NULL,
  workdir = tempfile("dfca_multi_"),
  verbose = FALSE
)
}
\arguments{
\item{sources, targets}{Named, equal-length lists of matched objects. \code{sources}
are templates (moving), \code{targets} the subject (fixed). Names become object ids.}

\item{kernel_width}{Deformation kernel width.}

\item{object_kernel_width}{Data-attachment kernel width — the spatial scale at which
each object's surface/curve mismatch is measured (Current/Varifold). \emph{Smaller =
finer, more local matching}, which pulls small structures (galls, noduli) onto
their target more tightly. A single value for every object, or a per-object vector
(recycled if length 1; matched by name to \code{sources} if named, else in order).
Defaults to \code{kernel_width}.}

\item{landmarks}{Optional \code{list(source=, target=)} of anchoring point matrices,
added as a shared Landmark object (the \code{inc_tracts_lmarks} regulariser).}

\item{data_sigma}{Data-attachment noise sigma. A single value applied to every
object, or a per-object vector (recycled if length 1; matched by name to
\code{sources} if named, else taken in order). \emph{Smaller sigma weights that object
more strongly} in the fit — e.g. give homologous neuropils a small sigma and
the outer hull a larger one so the neuropils drive the internal alignment.}

\item{landmark_sigma}{Data-attachment sigma for the optional shared \code{landmarks}
object (defaults to the mean of \code{data_sigma}).}

\item{timepoints, max_iterations, device, deformetrica, workdir, verbose}{As
\code{\link[=deformetrica_register]{deformetrica_register()}}.}
}
\value{
A list with \code{control_points}, \code{momenta}, \code{kernel_width}, \code{output_dir}.
}
\description{
The flyconnectome/deformetricaLR recipe, ported to Deformetrica 4: register a
whole SET of matched cognate objects (e.g. left/right neuron tracts) with a
single diffeomorphism, optionally anchored by a shared \code{landmarks} point cloud.
Objects may mix meshes, neurons (polylines) and point sets; each \code{sources[[i]]}
is matched to \code{targets[[i]]} by name.
}
\seealso{
\code{\link[=deformetrica_register]{deformetrica_register()}} for the single-object case.
}
