% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp-helpers.R
\name{affine_prealign}
\alias{affine_prealign}
\title{Affine pre-alignment of one object onto another}
\usage{
affine_prealign(
  source,
  target,
  type = c("rigid", "similarity", "affine"),
  iterations = 30L
)
}
\arguments{
\item{source, target}{Objects accepted by \code{\link[nat:xyzmatrix]{nat::xyzmatrix()}} (\code{mesh3d}, neuron,
dotprops or an N x 3 matrix). \code{source} moves onto \code{target}.}

\item{type}{ICP type passed to \code{\link[Morpho:icpmat]{Morpho::icpmat()}}: "rigid", "similarity" or "affine".}

\item{iterations}{ICP iterations.}
}
\value{
A list with \code{aligned} (the transformed \code{source}), \code{apply} (a function
mapping any object through the identical transform) and the components
\code{scale}, \code{centre_source}, \code{centre_target} and \code{icp} (the 4x4 ICP matrix).
}
\description{
A diffeomorphism should not have to absorb a gross change of pose and scale;
that is this step's job. Both objects are centred, \code{source} is isotropically
scaled to \code{target}'s bounding-box diagonal, and a rigid (or affine) ICP refines
the orientation. The returned \code{apply()} closure replays the \emph{same} transform on
any other object, so a whole set of sub-objects (e.g. neuropils) can be carried
into the aligned frame consistently.
}
\seealso{
\code{\link[=deformetrica_register]{deformetrica_register()}} for the non-rigid step that follows.
}
