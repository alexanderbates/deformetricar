% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deformetrica4.R
\name{deformetrica_shoot}
\alias{deformetrica_shoot}
\title{Apply a fitted Deformetrica diffeomorphism to 3D points (geodesic shooting)}
\usage{
deformetrica_shoot(
  x,
  control_points,
  momenta,
  kernel_width,
  timepoints = 10L,
  object_type = "Landmark",
  device = c("auto", "cpu", "cuda"),
  deformetrica = NULL,
  workdir = tempfile("dfca_shoot_"),
  flow = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{x}{An N x 3 matrix of coordinates, or anything \code{\link[nat:xyzmatrix]{nat::xyzmatrix()}} accepts.}

\item{control_points, momenta}{Path to the estimated Deformetrica
\code{...ControlPoints.txt} / \code{...Momenta.txt}, or a numeric matrix. Passing the
files Deformetrica itself wrote is strongly preferred: it writes momenta with
a leading \verb{n_subjects n_cp dim} header that its own reader expects, so a bare
matrix is best-effort only.}

\item{kernel_width}{Deformation kernel width, in the coordinate units. Larger =
stiffer / more global; must match the width the registration was fit with.}

\item{timepoints}{Number of geodesic integration steps; must match the fit.}

\item{object_type}{Deformetrica \code{deformable-object-type} (default "Landmark").}

\item{device}{\code{kernel-device}: "auto", "cpu" or "cuda".}

\item{deformetrica}{Optional path to the executable (auto-detected otherwise).}

\item{workdir}{Working directory for the run (a fresh tempdir by default).}

\item{flow}{If \code{TRUE}, return the \emph{whole} geodesic flow (every timepoint) as a
list of deformed objects (one per timepoint, named \code{tp_00}..\code{tp_NN}) rather
than just the final shape. Useful for animating a warp.}

\item{verbose}{Show \code{deformetrica} output.}
}
\value{
By default an N x 3 matrix of deformed coordinates (or a warped object
of the input class), in the input units and row order. With \code{flow = TRUE}, a
list of such objects, one per geodesic timepoint.
}
\description{
Flows arbitrary points through the diffeomorphism defined by a fitted
registration's control points and initial momenta, by writing a
\verb{<model-type>Shooting} model and calling \verb{deformetrica compute}.
}
