% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp-helpers.R
\name{mirror_lr_split}
\alias{mirror_lr_split}
\title{Assign left/right to an (even unpaired, midline) object by self-mirror warping}
\usage{
mirror_lr_split(
  x,
  axis = c("X", "Y", "Z"),
  mid = NULL,
  refine = FALSE,
  band = 0,
  positive_travel = c("L", "R"),
  ...
)
}
\arguments{
\item{x}{A \code{mesh3d}.}

\item{axis}{Mirror axis, "X" (default), "Y" or "Z".}

\item{mid}{Midline coordinate on \code{axis} (defaults to the extent midpoint).}

\item{refine}{If \code{TRUE}, refine the flat reflection with a Deformetrica warp of
\code{x} onto its reflection (finds a curved symmetry surface). Needs a working
Deformetrica install; passes \code{...} (e.g. \code{kernel_width}, \code{device}) through to
\code{\link[=deformetrica_register]{deformetrica_register()}}/\code{\link[=deformetrica_shoot]{deformetrica_shoot()}}.}

\item{band}{Fraction of the axis extent within which |travel| is treated as
midline (\code{"M"}) rather than L/R. Default 0 (every point is L or R).}

\item{positive_travel}{Which hemisphere a point that travels in the \code{+axis}
direction belongs to ("L" default, or "R").}

\item{...}{Passed to the Deformetrica calls when \code{refine = TRUE}.}
}
\value{
A list with \code{L}/\code{R} sub-meshes, a per-vertex \code{side} factor and the
signed \code{travel} along \code{axis}.
}
\description{
A mini use-case of the package. An object that straddles the midline (a fly
ellipsoid body, protocerebral bridge, ...) has no left/right label, yet every
point still belongs to a hemisphere. We recover that the way a mirroring
registration does (cf. \code{bancr::banc_lr}): reflect the object across a nominal
midline plane and, optionally, \code{refine} the reflection with a Deformetrica warp
of the object onto its own reflection so the true (possibly curved) symmetry
surface is found. Each point is then labelled by \emph{how far and in which direction
it must travel to reach its mirror partner} — points on the symmetry surface
barely move; points to one side travel toward the other.
}
\seealso{
\code{\link[=split_mesh_lr]{split_mesh_lr()}} for the plain-plane split.
}
