% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp-helpers.R
\name{split_mesh_lr}
\alias{split_mesh_lr}
\title{Split a surface mesh into left and right halves by a midline plane}
\usage{
split_mesh_lr(x, mid = NULL, axis = c("X", "Y", "Z"), left = c("low", "high"))
}
\arguments{
\item{x}{A \code{mesh3d}.}

\item{mid}{The midline coordinate on \code{axis}; defaults to the midpoint of the
mesh's extent on that axis.}

\item{axis}{Split axis, "X" (default), "Y" or "Z".}

\item{left}{A face on the side with the \emph{smaller} (\code{"low"}, default) or \emph{larger}
(\code{"high"}) \code{axis} coordinate is called the left half.}
}
\value{
A list with \code{L} and \code{R} sub-meshes and the \code{mid} used.
}
\description{
Faces are assigned to a side by their centroid position along \code{axis} relative
to \code{mid}. For a cleaner split of a shape whose symmetry surface is curved
rather than a flat plane, see \code{\link[=mirror_lr_split]{mirror_lr_split()}}.
}
