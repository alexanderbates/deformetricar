% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-deformetrica.R
\name{install_deformetrica}
\alias{install_deformetrica}
\title{Install Deformetrica into a managed Python environment}
\usage{
install_deformetrica(
  envname = "deformetrica",
  method = c("conda", "virtualenv", "auto"),
  python_version = "3.8",
  version = "deformetrica",
  extra_packages = "numpy",
  x86 = NA,
  force = FALSE,
  ...
)
}
\arguments{
\item{envname}{Name of the conda env / virtualenv to install into
(default \code{"deformetrica"}). A dedicated env is recommended because Deformetrica
pins specific \code{torch}/\code{pykeops} versions.}

\item{method}{One of \code{"conda"} (default, recommended — Deformetrica's own install
instructions use a conda env), \code{"virtualenv"}, or \code{"auto"} (conda if a conda
binary is found, else virtualenv).}

\item{python_version}{Python for the new environment (default \code{"3.8"}, which
matches the \code{torch==1.6} / \verb{pykeops==1.4.1} that Deformetrica 4.3 pins).}

\item{version}{Package spec passed to pip (default \code{"deformetrica"}; pin with e.g.
\code{"deformetrica==4.3.0"}).}

\item{extra_packages}{Extra packages to co-install (default \code{"numpy"}, per
Deformetrica's install notes).}

\item{x86}{Build an x86-64 (\code{osx-64}) conda env that runs under Rosetta. \code{NA}
(default) auto-enables this on Apple Silicon and disables it elsewhere. Needed on
arm64 macOS because Deformetrica's \code{torch==1.6} pin has no arm64 wheels; the
x86-64 wheels install and run fine under Rosetta.}

\item{force}{Reinstall even if a \code{deformetrica} executable is already present in
the environment.}

\item{...}{Passed to \code{\link[reticulate:conda-tools]{reticulate::conda_install()}} / \code{\link[reticulate:virtualenv-tools]{reticulate::virtualenv_install()}}.}
}
\value{
The path to the installed \code{deformetrica} executable, invisibly.
}
\description{
\code{deformetricar} shells out to the \code{deformetrica} (>= 4.3) command-line tool, so
you need that tool available once. This is the smooth path: it uses
\href{https://rstudio.github.io/reticulate/}{reticulate} to create (or reuse) a
dedicated Python environment, \verb{pip install}s Deformetrica into it, verifies the
\code{deformetrica} console script, and caches its path in
\code{options(deformetricar.exe=)} so every subsequent \code{\link[=find_deformetrica]{find_deformetrica()}} call
resolves it automatically (including in new sessions if you persist the option).
}
\section{Apple Silicon (arm64 macOS)}{
 Deformetrica 4.3 pins \code{torch==1.6}, which has
no native arm64 wheels, so a plain arm64 \verb{pip install} fails. This function
handles it for you: on an M-series Mac it defaults to \code{x86 = TRUE}, building an
\code{osx-64} conda env (Python 3.8) whose interpreter runs under Rosetta and pulls the
x86-64 wheels — verified end-to-end (estimate + compute) on this hardware. You
need Rosetta (\code{softwareupdate --install-rosetta}) and a conda binary; the first
\code{deformetrica} call per process pays a ~45 s Rosetta cold-start to import
torch/vtk, then runs normally.
}

\examples{
\dontrun{
# One-time setup (needs conda/Miniconda; reticulate::install_miniconda() gets one):
install_deformetrica()
# ... then the rest of the package just works:
find_deformetrica()
}
}
\seealso{
\code{\link[=find_deformetrica]{find_deformetrica()}} to locate an existing install.
}
