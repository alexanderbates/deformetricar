% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deformetrica4.R
\name{deformetrica_register}
\alias{deformetrica_register}
\title{Fit a Deformetrica diffeomorphism between two point sets (registration)}
\usage{
deformetrica_register(
  source,
  target,
  kernel_width,
  timepoints = 10L,
  object_type = "Landmark",
  attachment_type = c("Landmark", "Varifold", "Current"),
  noise_std = 0.01,
  max_iterations = 150L,
  device = c("auto", "cpu", "cuda"),
  deformetrica = NULL,
  workdir = tempfile("dfca_reg_"),
  verbose = FALSE
)
}
\arguments{
\item{source, target}{N x 3 matrices of corresponding points (row \emph{i} of
\code{source} matches row \emph{i} of \code{target}). Must have the same number of rows.}

\item{kernel_width}{Deformation kernel width (larger = stiffer / more global). This also
sets the CONTROL-POINT count: Deformetrica seeds control points on a grid spaced by
\code{kernel_width} over the objects' bounding box, so CP count grows as
(extent / kernel_width)^3. It is the knob for how many control points the warp has.
Beware a value that is small relative to the object EXTENT (e.g. a compact arbor with a
long soma tract): it spawns thousands of control points, over-parameterises the fit, and
the optimiser can collapse to zero momenta (identity warp). Size \code{kernel_width} to the
extent (for a sane CP count) and use the per-object \code{object_kernel_width} to set the
finer matching scale.}

\item{timepoints}{Geodesic integration steps.}

\item{object_type}{Deformetrica \code{deformable-object-type} (default "Landmark").}

\item{attachment_type}{"Landmark" (ordered L2, default), "Varifold" or "Current".}

\item{noise_std}{Data-attachment noise standard deviation.}

\item{max_iterations}{Optimiser iterations.}

\item{device}{\code{kernel-device}: "auto", "cpu" or "cuda".}

\item{deformetrica}{Optional path to the executable (auto-detected otherwise).}

\item{workdir}{Working directory for the run (a fresh tempdir by default).}

\item{verbose}{Show \code{deformetrica} output.}
}
\value{
A \code{deformetricareg} object (a portable handle on the fitted diffeomorphism,
storing the control points + momenta inline). Apply it with \code{nat::xform(x, reg)} or
\code{\link[=deformetrica_shoot]{deformetrica_shoot()}}; it survives \code{saveRDS()}. Its elements \code{control_points},
\code{momenta}, \code{kernel_width}, \code{timepoints} and \code{output_dir} are still accessible.
}
\description{
The Deformetrica 4 estimate path: fit a diffeomorphism that deforms \code{source}
(the moving template) onto \code{target} (the fixed subject). With ordered \code{source}
<-> \code{target} row correspondences a point-to-point Landmark attachment is used.
Returns the estimated control points + momenta, which \code{\link[=deformetrica_shoot]{deformetrica_shoot()}}
then applies to arbitrary new points.
}
\seealso{
\code{\link[=xformpoints.deformetricareg]{xformpoints.deformetricareg()}} / \code{\link[=deformetrica_shoot]{deformetrica_shoot()}} to apply the fit.
}
