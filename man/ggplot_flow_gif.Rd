% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp-helpers.R
\name{ggplot_flow_gif}
\alias{ggplot_flow_gif}
\title{Animate a Deformetrica geodesic flow as a nat.ggplot GIF}
\usage{
ggplot_flow_gif(
  flows,
  cols = NULL,
  volume = NULL,
  volume_alpha = 0.12,
  alpha = 0.6,
  rotation_matrix = NULL,
  file = NULL,
  width = 900,
  height = 800,
  delay = 0.14,
  dpi = 96
)
}
\arguments{
\item{flows}{A named list; each element is the per-timepoint list of objects for
one structure (all lists the same length). Colours are taken per structure.}

\item{cols}{Named vector of colours (one per \code{flows} entry); recycled from a
default palette if \code{NULL}.}

\item{volume}{Optional fixed reference object drawn translucent under every frame
(e.g. the target brain surface).}

\item{volume_alpha, alpha}{Alphas for the reference volume and the flow objects.}

\item{rotation_matrix}{Optional 4x4 view matrix (as \code{nat.ggplot}/\code{rgl} use).}

\item{file}{Output GIF path. Frames are written next to it.}

\item{width, height, delay, dpi}{GIF frame size (px), per-frame delay (s) and dpi.}
}
\value{
The GIF path if written (needs \code{gifski}), else the vector of frame PNGs.
}
\description{
Renders a set of warping objects across the timepoints of a geodesic flow (as
returned by \code{\link[=deformetrica_shoot]{deformetrica_shoot()}} with \code{flow = TRUE}) to a ping-pong GIF, each
object in its own colour over a translucent reference volume.
}
