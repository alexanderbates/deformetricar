% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp-helpers.R
\name{ggplot_flow_gif}
\alias{ggplot_flow_gif}
\title{Animate a Deformetrica geodesic flow as a nat.ggplot GIF}
\usage{
ggplot_flow_gif(
  flows,
  cols = NULL,
  volume = NULL,
  volume_alpha = 0.12,
  volume_col = "grey80",
  targets = NULL,
  target_cols = NULL,
  target_alpha = 0.18,
  alpha = 0.6,
  rotation_matrix = NULL,
  points = NULL,
  point_cols = NULL,
  point_size = 3,
  point_alpha = 1,
  point_stroke = 0.8,
  file = NULL,
  width = 900,
  height = 800,
  delay = 0.14,
  dpi = 96
)
}
\arguments{
\item{flows}{A named list; each element is the per-timepoint list of objects for
one structure (all lists the same length). Colours are taken per structure.}

\item{cols}{Named vector of colours (one per \code{flows} entry); recycled from a
default palette if \code{NULL}.}

\item{volume}{Optional fixed reference object drawn translucent under every frame
(e.g. the target brain hull).}

\item{volume_alpha, target_alpha, alpha}{Alphas for the reference volume, the target
objects and the flow objects.}

\item{volume_col}{Colour of the reference volume (default \code{"grey80"}; pass e.g. a
light pink to render the brain hull as a soft envelope).}

\item{targets}{Optional named list of fixed reference objects drawn translucent
\emph{above} the volume but \emph{below} the flow (e.g. the matched target neuropils each
warping object should land on). Static across frames.}

\item{target_cols}{Named vector of colours for \code{targets} (one per entry); defaults
to a transparent greyscale ramp so the coloured flow reads clearly on top.}

\item{rotation_matrix}{Optional 4x4 view matrix (as \code{nat.ggplot}/\code{rgl} use).}

\item{points}{Optional named list of point sets drawn as circles on top of the
neurons — e.g. neuron root points / somata. Each entry is either a static N x 3
matrix or a per-timepoint list of matrices (circles that move with the warp),
forwarded to \code{\link[nat.ggplot:ggneuron_gif]{nat.ggplot::ggneuron_gif()}}.}

\item{point_cols}{Named colours for \code{points}; \code{point_size} the circle size,
\code{point_alpha} its opacity and \code{point_stroke} its outline width.}

\item{file}{Output GIF path. Frames are written next to it.}

\item{width, height, delay, dpi}{GIF frame size (px), per-frame delay (s) and dpi.}
}
\value{
The GIF path if written (needs \code{gifski}, or falls back to \code{magick}), else
the vector of frame PNGs.
}
\description{
A thin convenience wrapper around \code{\link[nat.ggplot:ggneuron_gif]{nat.ggplot::ggneuron_gif()}} (where the general
"animate a sequence of nat objects to a GIF" machinery now lives). It renders a set
of warping objects across the timepoints of a geodesic flow (as returned by
\code{\link[=deformetrica_shoot]{deformetrica_shoot()}} with \code{flow = TRUE}) to a ping-pong GIF, each object in its
own colour over an optional reference volume and static greyscale targets. Kept for
convenience/back-compatibility; new code can call \code{\link[nat.ggplot:ggneuron_gif]{nat.ggplot::ggneuron_gif()}}
directly.
}
\seealso{
\code{\link[nat.ggplot:ggneuron_gif]{nat.ggplot::ggneuron_gif()}} for the underlying, general animation function.
}
